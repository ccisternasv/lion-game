import { MissingTranslationStrategy } from '@angular/core';

export class RndPosition {

    private _min = 0;
    private _max = 1;
    constructor(){

    }

    genRndInt():Number{
        return Math.random()+;
    }
}
